import React from "react";
import "./Clone.css";

const Clone = () => {
  return (
    <section className="clone" id="clone">
      <div className="clone-inner">
        <span className="clone-badge">CLONE</span>
        <h2 className="clone-title">
          <span className="clone-title-italic">CLONE</span>{" "}
          <span className="clone-title-main">PROJECT</span>
        </h2>
        <p className="clone-sub">Clone section placeholder.</p>
      </div>
    </section>
  );
};

export default Clone;
